name: "build"
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Build with Emscripten"
      - uses: mymindstorm/setup-emsdk@v11

      - name: Verify
        run: emcc -v
